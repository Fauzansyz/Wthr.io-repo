<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Mengubah warna Status Bar menjadi putih -->
        <item name="android:statusBarColor">@color/abu-abu</item>

        <!-- Mengubah teks dan ikon di Status Bar menjadi hitam -->
        <item name="android:windowLightStatusBar">false</item>

        <!-- Mengubah warna Navigation Bar menjadi putih -->
        <item name="android:navigationBarColor">@color/abu-abu</item>

        <!-- Mengubah teks dan ikon di Navigation Bar menjadi hitam -->
        <item name="android:windowLightNavigationBar">false</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/color-mode</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>